SpotifyPlayer.swift:
* `QueueItem` looks very similar to `Track` from Entities.swift, and we also have `APITrack`, and then also `TrackRowData` and `TrackMetadata`. why do we need 4 very similar structs? can't we just have 1? or at least fewer than we have now? we do convert one into the other (search for "Convert TrackCodable to QueueItem" in SpotifyPlayer.swift to see an example), can't we just establish a naming convention for Track attributes that is derived from Spotify API official names used everywhere? there might be a problem with the externalUrl for sharing/copying to clipboard, but if this value is not set for a track (also let me know which APIs/callbacks don't supply this, maybe we can avoid this API!), we can use a global helper to convert the id/uri to an external url.
* we also have `Playlist` and `APIPlaylist`, `Device` and `SpotifyDevice`. does it make sense to unify these? also check for other potentially duplicated structs, like for Albums.
* Is it possible to move the `LibrespotConnectionState` into the global store (to the ConnectionService) and to reference it from SpotifyPlayer? Does this have any downsides?
* when we run `SpotifyAPI.fetchQueue`, we update the `PlaybackState` with 2 attributes that are not available from this endpoint, `positionMs` and `shuffle`. is it possible to do a more fine grained update, leaving those 2 values alone? i mean, to leave them at their previous value, without statically setting the one to 0 and the other to false?
